当前请严格按以下规则提取信息，输出结构化Markdown：

### 📌 核心规则
1. **标题层级**：严格使用“业务架构”中的中文标题（#/##/###/####/#####/######）
2. **仅处理业务架构中最深层级（即无子标题）的中文标题**。若某标题下还包含更细粒度的子标题，则该标题**不视为有效提取目标，必须跳过**。
3. **内容填充**：
   - 仅当原文**明确出现**且**符合下方“字段细则”要求**的信息时，用`● 原文片段（来源：文档位置）`填充
   - 无匹配信息时：该字段标题下**完全留空**（禁止写“无”“未提及”等）
4. **内容要求**：
   - 直接复制原文片段，禁止改写/总结/补充/主观推断
   - **提取时尽可能保留原文的完整表述，不得对原文进行拆分、合并、截断或重组**
5. **输出范围**：仅Markdown内容，无前缀后缀、无解释

### 🔑 字段提取细则（必须逐条遵守！）
{{ field_specs }}

### 🌐 业务架构（输出框架）
{{ schema }}

{{ example }}

### 📝 待提取文本
{{ text }}